 {% extends "base.html.twig" %}

{% block title %}Home |
{{ parent() }} 
{% endblock %}

 {% block central %}
 <h1>Liste</h1>

<a href="{{ path('ajouter') }}">Ajouter wish</a>
<table class="table">

  <thead>
    <tr>
      <th scope="col">id</th>
      <th scope="col">Title</th>
    </tr>
  </thead>
      {% for w in wishes %}
  <tbody>
    <tr>
      <th scope="row">{{ w.id}}</th>
      <td>{{ w.title }}</td>
    </tr>
    <tr>
  </tbody>
  {% endfor %}
</table>

 {% endblock %}